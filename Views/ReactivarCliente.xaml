<Window x:Class="primer_wpf.Views.ReactivarCliente"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:primer_wpf.Views"
        mc:Ignorable="d"
        Activated="Window_Activated"
        Title="Reactivar Cliente" Height="450" Width="800" Background="LightSteelBlue">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="Clientes desactivados"
               FontSize="18"
               FontWeight="Bold"
               FontStyle="Italic"
               HorizontalAlignment="Center"
               Margin="5"/>

        <DataGrid Name="dataGridClientesInactivos"
                  Grid.Row="1"
                  Margin="10"
                  AutoGenerateColumns="False"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  CanUserAddRows="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <!--<DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>-->
                <DataGridTextColumn Header="Nombre" Binding="{Binding nombre}" Width="*"/>
                <DataGridTextColumn Header="Apellido" Binding="{Binding apellido}" Width="*"/>
                <DataGridTextColumn Header="Teléfono" Binding="{Binding telefono}" Width="*"/>
                <DataGridTextColumn Header="Dirección" Binding="{Binding direccion}" Width="*"/>
                <DataGridTextColumn Header="Código cliente" Binding="{Binding cod_cliente}" Width="100"/>
                <DataGridTextColumn Header="Estado" Binding="{Binding Activo}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Name="btnReactivar"
                Grid.Row="2"
                Content="Reactivar Cliente"
                Width="200" Height="40"
                HorizontalAlignment="Center"
                Margin="10"
                Click="btnReactivar_Click"/>
    </Grid>
</Window>
